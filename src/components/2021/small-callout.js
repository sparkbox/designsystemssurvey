import React from "react"
import PropTypes from "prop-types"

const SmallCallout = (props) => {
  return (
    <div className="cmp-small-callout">
      {props.children}
    </div>
  )
}

export default SmallCallout
